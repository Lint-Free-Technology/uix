homeassistant:
  country: US
  unit_system: metric
  time_zone: UTC
  media_dirs:
    media: /config/media

default_config:

demo:

lovelace:
  mode: storage

frontend:
  themes: !include themes.yaml
