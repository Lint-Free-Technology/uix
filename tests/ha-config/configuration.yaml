homeassistant:
  country: US
  unit_system: metric
  time_zone: UTC
  customize: !include customize.yaml

default_config:

demo:

lovelace:
  mode: storage
  resource_mode: yaml
  resources: !include lovelace_resources.yaml

frontend:
  themes: !include themes.yaml

input_boolean:
  test_boolean:
    name: Test Boolean
    initial: off

person:
  - name: James Bond 007
    id: james_bond007